Remove inheritance implemenation from ConfigUtility

ConfigUtility

Load	(fName)
	-load a config file, formatted as a set of keys and elements

Integer	(key)
Real	(key)
Boolean	(key)
	-return the respective types, or throw an exception

ConcatKeys(count, ...)
	-concat a series of elements, and return them as a singel string
	is this really needed?: config["path"] + config["file"]

operator[](key)
	-get an element

GetMap()
	-return the map member, basic OO breaking accessor

-------------------------

"Target" option in CameraUtility, targeting an origin?

Camera2DUtility

GetCamX
GetCamY

SetMode
GetMode

ScreenSize

SetTarget(Origin2D*)
GetTarget(Origin2D*)

-mode

-screenw
-screenh

-Origin2D* target
